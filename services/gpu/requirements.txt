# Lineup GPU container — extra Python deps installed on top of the
# AWS DLC pytorch-inference base image.
#
# pyannote.audio 4.x is required for pyannote/speaker-diarization-community-1.
# Pyannote 4 pulls torch>=2.8, torchcodec, and pyannoteai-sdk transitively —
# pip will upgrade the DLC's bundled torch (2.4) to satisfy the constraint.
# On Linux this is fine; torchcodec ships FFmpeg shared libs and the runtime
# stage already installs ffmpeg via apt for our subprocess uses.
#
# Local dev (services/api) stays on pyannote 3.x — the analyst code uses
# the _pyannote_compat.token_kwargs() shim to handle the renamed auth kwarg.
pyannote.audio>=4.0,<5
insightface>=0.7,<0.8
onnxruntime-gpu>=1.17,<2
opencv-python-headless>=4.8,<5
boto3>=1.36,<2
sqlalchemy==2.0.*
psycopg[binary]==3.2.*
pydantic==2.10.*
pydantic-settings==2.7.*
deepgram-sdk>=3.7,<4
pgvector>=0.3,<1
