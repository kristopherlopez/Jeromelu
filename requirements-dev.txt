pytest>=8.0
# Pyright. Scope is configured in the root pyproject.toml [tool.pyright]
# block (narrow to packages/shared). Range here; CI tests.yml pyright job
# pins the exact version after `pip install -r requirements-*.txt` for
# determinism (pyright ships frequent releases).
pyright>=1.1.380,<2
# openai / temporalio are imported at module top-level inside the pyright
# include set (packages/shared/jeromelu_shared/llm.py + temporal.py), so
# pyright needs them present to resolve imports. requirements-test.txt
# deliberately excludes them (its docstring lists "network SDKs" as
# excluded for fast test installs) so they live here instead. Versions
# match services/api/requirements.txt.
openai==1.65.*
temporalio==1.9.*
